name: custom_logger
description: >
  A comprehensive Dart project that includes custom logger functionality
  for enhanced console output formatting. It provides options to set log
  levels, color codes, prefixes, suffixes, and more, allowing for tailored
  logging behavior.
version: 1.0.0
homepage:

environment:
  sdk: '>=3.2.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  firebase_analytics: ^10.8.5
  firebase_crashlytics: ^3.4.14
  flutter:
    sdk: flutter
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:


